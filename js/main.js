/* =====================================================================
   BOOT — runs last, once every other script has loaded.
   ===================================================================== */
renderLessonPick();
renderLesson();
render();
